services:
  nginx:
    image: nginx:stable-alpine
    container_name: futebol_nginx
    ports:
      - "8080:80"
    volumes:
      - ./src:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php
    networks:
      - futebol_network

  php:
    build:
      context: .
      dockerfile: ./docker/php/Dockerfile
    container_name: futebol_php
    working_dir: /var/www/html
    volumes:
      - ./src:/var/www/html
    depends_on:
      - mysql
    networks:
      - futebol_network

  mysql:
    image: mysql:8.0
    ports:
      - "3308:3306"
    container_name: futebol_mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: db_futebol
      MYSQL_USER: user
      MYSQL_PASSWORD: user
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - futebol_network

networks:
  futebol_network:
    driver: bridge

volumes:
  mysql_data:
